<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,500;0,700;1,500&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="/stylesheets/admin.css">
        <script src="https://kit.fontawesome.com/720e4c8677.js" crossorigin="anonymous"></script>
        
    
        <title>Pint@me</title>
    </head>
<body>
    <header class="cabecera">
        <a href="/" class="logo"><img src="./images/logos/Pintamev2.png" alt="" ></a> 
    </header>
    
    <section class="contenedor">
        <div class="ingreso">
            <h1>BIENVENIDO AL SITIO DE ADMINISTRADOR</h1><hr  class="hrcolor">
            <h2>Ingresar a mi cuenta de administrador</h2> 
            
            <h4>Ingresá tu dirección de email y contraseña para iniciar sesión.</h4>
            <form action="/admin" method="POST" id= "registro" name= "registro" class= "registro" >
                <label for="email">Correo electronico</label><br>
                <input id= 'email' type="email" name="email"  value="<%= (typeof old != 'undefined' && !errors.email) ? old.email : '' %>" placeholder="Ingresa tu email"><br>
                <small class="text-danger" style="color: red"><%= (typeof errors != 'undefined' && errors.email) ? errors.email.msg : '' %></small> <br>
               
                <label for="pass">Contraseña</label><br>
                <input id = "pass"  type="password" name="pass" value="<%= (typeof old != 'undefined' && !errors.pass) ? old.pass : '' %>" placeholder="Tú clave"> <br>
                <small class="text-danger" style="color: red"><%= (typeof errors != 'undefined' && errors.pass) ? errors.pass.msg : '' %></small>
            
                <div class="recordar">
                <label class="recordame">Recordame &nbsp </label>  <input type="checkbox"  id="recordamecheck" class="recordamecheck">       
                 </div>
                <br>
              
                <a href="/recupero" class="recuperar"> <small>Recuperar Contraseña</small></a> <br>

                <button type="submit" name="entrar" class="enviar" onclick="">Enviar</button>
            </form>
        </div>

        

        
    </section>
    <%- include('partials/Footer.ejs') %>    

   
</body>